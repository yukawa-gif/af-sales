# AF Sales 開発メモ

## このプロジェクトについて

エー・ファクトリー株式会社の営業管理ツール開発。
2026年4月に一時中断後、**同月に開発再開**。

---

## 開発環境

| 項目             | 内容                                                                                                              |
| ---------------- | ----------------------------------------------------------------------------------------------------------------- |
| リポジトリ       | https://github.com/yukawa-gif/af-sales.git                                                                        |
| 本番URL          | https://af-sales.vercel.app                                                                                       |
| デプロイ         | GitHubのmainにpushすると自動でVercelに反映                                                                        |
| GAS URL          | `https://script.google.com/macros/s/AKfycbw_Hx1yajz8zFkpjoTs3YdAR3AdSPHG8VGDzg5h5xiZUid80rC1gSq-uRPX6BAZJOM/exec` |
| スプレッドシート | https://docs.google.com/spreadsheets/d/1eW1tYD_FYih6w4HYpgayXk2JO0r3UEfXFQNo1jcfm28/edit                          |

---

## 作成済みファイル

| ファイル            | 内容                                                        | 状態         |
| ------------------- | ----------------------------------------------------------- | ------------ |
| `dashboard.html`    | 営業管理ツール本体（KPI・月別推移・インセンティブ・AI分析） | 稼働中       |
| `deal_form.html`    | 案件登録フォーム                                            | 稼働中       |
| `daily_report.html` | 日報入力フォーム                                            | 稼働中       |
| `gas_v3.js`         | GASバックエンド（Gitには含まれない・GASエディタで管理）     | デプロイ済み |

---

## スプレッドシート構成

- `設定_担当者`：担当者名・役職・ステータス（在籍中/前任者）
- `設定_商材`：**変更予定あり**（下記参照）
- `設定_目標`：目標データをJSON形式で保存
- `営業実績`：案件登録データが蓄積
- `顧客DB`：顧客情報（AV列に前任者履歴を追加済み）

### 設定\_商材 新列構成（変更予定・未対応）

| 列  | 項目名                | 値                  |
| --- | --------------------- | ------------------- |
| A   | 商材名                | 例：HubCast\_直販   |
| B   | 種別                  | ストック / ショット |
| C   | 売上単価（円）        | 例：35000           |
| D   | 費用（円）            | 例：13000           |
| E   | インセンティブ率（%） | 例：10              |
| F   | 価格タイプ            | 固定 / 都度見積もり |

ストック = 12ヶ月契約、月別に計上。インセンティブは毎月支払い。
ショット = 当月のみ計上。インセンティブは初月のみ。
→ **スプレッドシートの変更後、GAS・deal_form.html・dashboard.htmlの修正が必要。**

---

## 登録済みの担当者

| 氏名     | 役職コード        |
| -------- | ----------------- |
| 鈴木純平 | M（マネージャー） |
| 坂井竜世 | SS                |
| 佐々木憩 | S                 |
| 濱田雄一 | S                 |
| 田中準子 | S                 |
| 綾戸涼太 | A                 |
| 湯川悦英 | P                 |

役職P（湯川）は目標設定・インセンティブ計算の対象外。

---

## 登録済みの商材（設定\_商材シート・現行）

| 商材名                   | 売上      | 費用    | 月数 |
| ------------------------ | --------- | ------- | ---- |
| リスキリング研修\_直販   | 200,000   | 0       | 1    |
| リスキリング研修\_代理店 | 200,000   | 150,000 | 1    |
| HubCast\_直販            | 35,000    | 13,000  | 12   |
| HubCast\_代理店          | 35,000    | 27,000  | 12   |
| IT導入補助金             | 1,000,000 | 0       | 1    |
| 社長紹介\_直販           | 600,000   | 0       | 1    |
| 社長紹介\_代理店         | 600,000   | 200,000 | 1    |
| 健康診断事務代理         | 0         | 0       | 1    |
| 保健師電話健康相談       | 0         | 0       | 1    |
| 社労士案件               | 0         | 0       | 1    |

**→ 商材マスタースプレッドシートの17商材に更新予定（要確認事項あり）**

---

## 確度ランクの定義

| ランク | 意味             |
| ------ | ---------------- |
| 売上   | 確定・入金済み   |
| 決定   | 受注確定・未入金 |
| A      | 高確度見込み     |
| B      | 中確度見込み     |
| C      | 低確度見込み     |
| 失注   | 失注確定         |

**ダッシュボードの「売上合計」はExcelの`=SUMIF(決定)+SUMIF(売上)`相当**（決定+売上の合算）。

---

## 粗利の計算式

```
粗利（単月）= 売上 - 費用
総粗利 = 粗利 × 月数 × コース数 × 数量
```

インポート時は**粗利を直接入力**（売上・費用は参考値）。
GASが利益を計算するのではなく、入力値の粗利をそのまま使う設計。

---

## 実装済みの主要機能

### dashboard.html

- チーム/個人切り替えビュー
- 月別推移グラフ（ヨミ合計行あり）
- 担当者別詳細パネル（個人選択時のみ表示）
  - ランクフィルタボタン（全件/売上/決定/A/B/C/失注）
  - 案件テーブル（行クリックで編集モーダル）
- 目標設定モーダル（M/SS/S/A 対象、P除外）
- インセンティブカード表示

### gas_v3.js（GASバックエンド）

- `getDeals()` - 案件データ取得（normYM関数でDate型対応済み）
- `addDeal()` / `updateDeal()` / `deleteDeal()` - CRUD
- `importFromSheet()` - インポート用シートから一括登録
  - 9列：担当者/会社名/確度ランク/売上予定月/売上/費用/粗利/商材名/メモ
  - 重複チェック：同担当者+会社名+月は登録しない
  - 粗利は入力値をそのまま使用（再計算しない）

---

## 重要な技術的知見

### GAS日付型のバグ対策

スプレッドシートのDate型は `instanceof Date` で判定できない（GAS環境のバグ）。
必ずduck typingで判定する：

```javascript
typeof v.getFullYear === 'function';
```

### localStorageは使わない

以前localStorageをキャッシュに使っていたが廃止。GASスプレッドシートを唯一のデータソースとする。

### file://プロトコルは使えない

`file://`でdashboard.htmlを開くとGASへのfetchがセキュリティエラーになる。
必ずローカルサーバー経由で動作確認：

```bash
python -m http.server 8080
# → http://localhost:8080/dashboard.html
```

### GASシートの列順序は変えない

`getMaster()`と書き込み系関数で列インデックスを直接参照しているため、
列を追加する場合は末尾に追加すること。

---

## 現在進行中の作業

### データ統一作業（週末締め切り）

全担当者がExcelの案件データを以下9項目に統一して提出：

| #   | 項目       | 備考                                                     |
| --- | ---------- | -------------------------------------------------------- |
| 1   | 担当者名   |                                                          |
| 2   | 会社名     |                                                          |
| 3   | 確度ランク | 売上/決定/A/B/C/失注                                     |
| 4   | 売上予定月 | yyyy-MM形式                                              |
| 5   | 売上       |                                                          |
| 6   | 費用       |                                                          |
| 7   | 粗利       | **この列が正**（売上・費用が不明な場合もここだけ正確に） |
| 8   | 商材名     |                                                          |
| 9   | メモ       |                                                          |

提出後に`importFromSheet()`で一括インポート予定。

### 商材マスター更新（完了）

設定\_商材シートを新列構成（A:商材名 B:種別 C:売上単価 D:費用 E:インセンティブ率 F:価格タイプ）に変更済み。
約120商材を登録済み。GAS・deal_form.html・dashboard.htmlの対応も完了。

## 技術的な学びと注意点

### GAS（Google Apps Script）連携

- `mode: 'no-cors'` でPOSTするとレスポンスが読めない → 成功/失敗の確認ができない
- GASを変更したら必ず**再デプロイ**が必要（忘れると古いコードが動く）
- シートの**列順序**は一度決めたら変えない（getMaster()と列順が食い違うとバグになる）

### localStorageの扱い

- localStorageは「キャッシュ」であって「DB」ではない
- 顧客DBのような大きなデータをlocalStorageに保存すると `QuotaExceededError` が発生する
  → `localStorage.setItem()` は必ず `try-catch` で囲む。保存失敗でもデータ取得は成功させる
- **GASを正として、同期時は完全置換する設計**が正しい（部分更新は危険）

### 商材データ（v3）

- 種別（ストック/スポット）から月数を自動導出：ストック=12、スポット=1
- インセンティブ計算：`incentiveRate`（数値）を直接使用。スプレッドシートのパーセント書式（0.10%）はGASが0.001として読む
- **計算式**：`incentive = monthlyGP × months × incentiveRate`
- 価格タイプ：空欄の場合、unitPrice > 0 → 固定、unitPrice = 0 → 都度見積もり（GASで自動判定）
- **正しい計算式**: 粗利（月次）= (売上単価 - 費用) × コース数 × 件数 / 総粗利 = 粗利（月次）× 月数

### Vercelデプロイ

- ローカルファイルを編集しても本番には反映されない
- `git push origin main` して初めてVercelに反映される（1〜2分かかる）
- ブラウザのキャッシュが残る場合は `Ctrl+Shift+R` で強制リロード

### 設計の反省（前回中断時）

- 出発点：「営業の目標設定・達成管理」（担当者起点）
- 途中からの実態：「顧客DB管理・担当者引き継ぎ管理」（顧客起点）
- この2つは土台が違う。顧客DBはスプレッドシートのまま運用で十分。

---

## 2026-04-24 実装内容

### dashboard.html に追加した機能

#### Googleタスク連携（+Tボタン）
- 案件テーブルの各行に `+T` ボタンを追加
- クリックで「Googleタスクに追加」モーダルが開く（タイトル自動入力・期日・メモ）
- GAS: `addTask(d)` 関数を追加（Tasks Advanced Service 使用）
- GAS action: `addTask` → `Tasks.Tasks.insert(task, '@default')`

#### 案件一覧の開閉機能
- 案件一覧ヘッダークリックで開閉トグル
- `▲/▼` アイコンで状態表示

#### フォローアップメール下書き機能（メールボタン）
- 対象：A/B/C ランクで最終更新日が14日以上前（または未記録）の案件、および決定・失注ランクの全案件
- 停滞行はオレンジ背景（`.row-stale`）でハイライト
- 「メール」ボタンクリックで Gemini がメール文草を自動生成
- Google Drive で会社名を含むドキュメントを検索し会議メモとして参照
- 「Gmailで開く」で Gmail 作成画面を自動入力して開く

#### GAS に追加した関数・機能
- `addTask(d)` : Google Tasks にタスクを登録
- `draftFollowUp(companyName, personName)` : メール下書き生成
  - 顧客DB C列（企業名）で照合、F列（担当者メールアドレス）を取得
  - Drive検索で会議メモ取得（会社名をファイル名に含むGoogleドキュメント）
  - Gemini API（gemini-2.5-flash）でメール文草をJSON形式で生成
- `doGet` に `mode=followup` を追加
- `addDeal()` / `updateDeal()` に最終更新日（Z列）の記録を追加

### スプレッドシートの変更
- 案件マスタ Z列（26列目）に「最終更新日」列を追加（※CLAUDE.mdの旧記載AW/49列目は誤り）

---

## シート構造メモ（2026-04-24 確認）

### 案件マスタ（シート名：案件マスタ）
| 列 | 項目 |
|----|------|
| A | 案件ID |
| B | 登録日 |
| C | 担当者 |
| D | 顧客ID（現在空欄） |
| E | 会社名 |
| F | 商材名 |
| G | フェーズ |
| H | 確度ランク |
| I | 売上（単価） |
| J | 費用（単価） |
| K | コース数 |
| L | 件数 |
| M | 月数 |
| N | 売上予定額（合計） |
| O | 費用（合計） |
| P | 粗利（合計） |
| Q | インセンティブ |
| R | 売上予定月 |
| S | 入金ステータス |
| T | 入金確認日 |
| U | メモ |
| V | 引継担当者 |
| W | 引継日 |
| X | 担当者コード |
| Y | 理由 |
| Z（26列目）| 最終更新日 |
| AA（27列目）| 計上会社 |
| AB（28列目）| B売上単価 |
| AC（29列目）| B費用単価 |
| AD（30列目）| B件数 |

### 顧客DB（シート名：顧客DB）
| 列 | 項目 |
|----|------|
| A | 顧客コード |
| B | AFC営業担当 |
| C | 企業名 |
| D | 担当者名 |
| E | 担当者電話番号 |
| F | 担当者メールアドレス |
| G | 都道府県 |
| H | 住所 |

※ 顧客DBは旧来のデータ（鍼灸・整骨院系）で案件マスタとほぼ非連携。**新・顧客マスタの構築が次の課題。**

---

## 2026-05-08 実装内容

### deal_form.html に追加した機能

#### 計上会社フィールド
- 担当者の下に「計上会社」セレクトを追加（必須項目）
- GAS `getCompanies()` で設定\_会社シートから動的取得
- `doGet` の `mode=master` レスポンスに `companies` を追加

#### B行（複合商材対応）
- 対象商材：社労士顧問・リスキリング・嘱託産業医など（設定\_商材のG・H列に値があるもの）
- 商材選択時に自動でB行を表示/非表示（`bUnitPrice > 0` で判定）
- B行：売上B単価・費用B単価・件数B（月数はA行と共有）
- 総粗利 = (A月次粗利 + B月次粗利) × 月数

### スプレッドシートの変更
- `設定_会社` シート新設（A:会社ID / B:会社名 / C:区分 / D:ステータス）
- `設定_商材` G列（B売上単価）・H列（B費用単価）追加
- `案件マスタ` AA〜AD列追加（計上会社・B売上単価・B費用単価・B件数）

### GAS の変更
- `getCompanies()` 関数追加（設定\_会社シートを読み込む）
- `getMaster()` の productDetails に `bUnitPrice`・`bCost` 追加
- `addDeal()` の appendRow を AA〜AD列まで拡張（row[26]〜row[29]）
- ※ `updateDeal()` の AA〜AD列対応は未実装（次の作業）

### 設定\_会社シート（6社）
| 会社ID | 会社名 | 区分 |
|--------|--------|------|
| C001 | エー・ファクトリー株式会社 | 株式会社 |
| C002 | サテライトサポート | 株式会社 |
| C003 | SORA | 株式会社 |
| C004 | ワイズフォー | 株式会社 |
| C005 | シンカ社会保険労務士 | 社労士 |
| C006 | エー・ファクトリー社労士 | 社労士 |

---

## 2026-05-09 実装内容

### GAS updateDeal() AA〜AD列対応

`updateDeal(d)` に以下のフィールドを追加（GASエディタで変更・再デプロイ必要）：

```javascript
// updateDeal(d) 内の sheet.getRange(...).setValues([row]) の該当行に追記
// AA列(row[26]):計上会社 / AB列(row[27]):B売上単価 / AC列(row[28]):B費用単価 / AD列(row[29]):B件数

function updateDeal(d) {
  const sheet = ss.getSheetByName('案件マスタ');
  const rows = sheet.getDataRange().getValues();
  for (let i = 1; i < rows.length; i++) {
    if (String(rows[i][0]) !== String(d.id)) continue;
    const row = rows[i];
    if (d.rankLabel    !== undefined) row[7]  = d.rankLabel;
    if (d.expectedMonth!== undefined) row[9]  = d.expectedMonth;   // I列 or 対応列に合わせて要確認
    if (d.grossProfit  !== undefined) row[11] = Number(d.grossProfit);
    if (d.memo         !== undefined) row[22] = d.memo;
    if (d.reason       !== undefined) row[24] = d.reason;
    if (d.paymentStatus!== undefined) row[23] = d.paymentStatus;
    // AA〜AD列（インデックス26〜29）
    if (d.billingCompany !== undefined) row[26] = d.billingCompany;
    if (d.bUnitSales     !== undefined) row[27] = Number(d.bUnitSales) || 0;
    if (d.bUnitCost      !== undefined) row[28] = Number(d.bUnitCost)  || 0;
    if (d.bQty           !== undefined) row[29] = Number(d.bQty)       || 0;
    row[25] = new Date(); // Z列: 最終更新日
    sheet.getRange(i + 1, 1, 1, row.length).setValues([row]);
    return { success: true };
  }
  return { success: false, error: 'not found' };
}
```

> ※ 列インデックスは現行 GAS の実装に合わせて確認すること。列番号がずれている場合は修正する。

### getDeals() の返却フィールド追加

`getDeals()` で案件マスタを読む際、AA〜AD列を以下の日本語キーで返す：

```javascript
'計上会社': row[26] || '',
'B売上単価': Number(row[27]) || 0,
'B費用単価': Number(row[28]) || 0,
'B件数':     Number(row[29]) || 0,
```

### dashboard.html に追加した機能

#### 案件編集モーダルに計上会社・B行フィールド追加
- 編集モーダル内に「計上会社」セレクトを追加（masterData.companies から動的生成）
- B行フィールド（B売上単価・B費用単価・B件数）を追加（B件数 > 0 の案件のみ表示）
- saveDealModal() が billingCompany / bUnitSales / bUnitCost / bQty を updateDeal に送信

#### 会社フィルター
- 月別推移タイムラインの上部に会社フィルターを追加
- ボタン：全社 / 株式会社（4社合計）/ 社労士（2社合計）/ 個社6社
- フィルター選択時は deals の '計上会社' 列で絞り込み、グループ合計行は常に表示

---

## 次の作業（TODO）

1. ~~**商材DB再設計**：スプレッドシートの「設定\_商材」を新列構成に変更~~ ✅ 完了
2. ~~**GAS更新**：新列構成に対応した `getMaster()` に修正・再デプロイ~~ ✅ 完了
3. ~~**deal_form.html**：月数を種別から自動設定、インセンティブ計算を簡略化~~ ✅ 完了
4. ~~**dashboard.html**：インセンティブカードのストック型対応（12ヶ月分をチェック）~~ ✅ 完了
5. ~~**deal_form.html**：計上会社フィールド・B行（複合商材）追加~~ ✅ 完了
6. ~~**updateDeal() の AA〜AD列対応**：編集時に計上会社・B行が保存されるよう修正~~ ✅ 完了（GASは別途デプロイ要）
7. ~~**dashboard.html**：月別推移グラフに会社フィルター追加（全社/株式会社/社労士/個社）~~ ✅ 完了
8. **データ統一作業**：全担当者のExcelデータを9項目に統一 → `importFromSheet()` で一括インポート（下記「引き継ぎ」参照）
9. **新・顧客マスタの構築**：案件マスタを中心に顧客データを再設計（別プロジェクトで実施）

---

## 2026-05-11 引き継ぎメモ

### 本日実施したこと

- `origin/claude/af-sales-continuation-t2Yqc` を `main` にマージ・push済み
  - deal_form.html：計上会社フィールド・B行（複合商材）が本番反映済み
  - dashboard.html：編集モーダルへの計上会社・B行追加、月別推移の会社フィルターが本番反映済み

### 未決定事項：データ一括インポートのテンプレート

営業担当者から既存データをもらってシステムに登録する作業が残っている。

**現在の `importFromSheet()` は9列固定：**
```
担当者 / 会社名 / 確度ランク / 売上予定月 / 売上 / 費用 / 粗利 / 商材名 / メモ
```

**新機能（計上会社・B行）を含めたいなら列追加が必要：**
```
担当者 / 会社名 / 確度ランク / 売上予定月 / 売上 / 費用 / 粗利 / 商材名 / メモ / 計上会社 / B売上単価 / B費用単価 / B件数
```

→ **ユーザーの判断待ち**：9列のまま進める（計上会社・B行は後から編集モーダルで手入力）か、13列に拡張してGASも更新するか。

### 売上データ/フォルダについて

`売上データ/` 配下のxlsxファイル（鈴木純平.xlsx 等）は **各担当者が元々使っている月次管理Excel**。
Claude が作ったテンプレートではなく、複数年分の月次集計シートを含む複雑な構成になっている。
インポート用の9列フォーマットに揃えて提出してもらう段取りは **まだ完了していない**。

### GASデプロイ状況

`updateDeal()` の AA〜AD列対応（計上会社・B行の保存）は CLAUDE.md に実装コードを記載済みだが、
**GASエディタへの適用・再デプロイが完了しているか要確認**。deal_form.html からの新規登録（addDeal）は対応済み。
